#include "Rational.h"

using namespace std;

int main(int argc, char** argv) {
	int size;
	cin >> size;
	int * array = new int[size];
	cout << array[0] << endl;
	//Rational a(6, 9), b(12, 72);
	//cout << a + b << endl;
	cin >> size;
	cout << "press any key to continue..." << endl;
	getchar();
	return 0;
}